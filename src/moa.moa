class Token {
  code   str
  file   str
  lineno int
  column int
}

class Node {
  token Token
}

def generate nodes {
  def genjs node {
  }
  return nodes.map(genjs).join("\n")
}

def tokenize program file {
  let reg r/(r\/(?:[^\/\\]|\\.)*?\/[A-Za-z]*|[0-9]+|(?:\.\.\.)?[A-Za-z0-9_]+|[\.()[\]{}]|"(?:[^"\\]|\\.)*"|`(?:[^`\\]|\\.)*`|[+\-*\/%<>!=^|&]+|#[^\n]*| +|\n+)/g
  return []
}

test t {
}

def parse tokens {
  return []
}

def infer nodes {
  return nodes
}

test t {
}
