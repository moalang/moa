def run source:
  eval(parse(tokenize(source)))

def eval node:
  node.float

def parse tokens:
  tokens.0

def tokenize source:
  list(source)

test t:
  def eq expectation source:
    t.eq(run(source) expectation)
  eq(1 "1")
  eq(1.2 "1.2")
