function __equal(a, b) {
  return (a.__type && a.__type === b) || (JSON.stringify(a) === JSON.stringify(b))
}
function __match(target, ...patterns) {
  if (target.__type) {
  } else {
  }
  return (a.__type && a.__type === b) || (JSON.stringify(a) === JSON.stringify(b))
}
