[] Design error handling type and sequence syntax
   - nullable
   - failable
   - if / for / while / break / continue / return / goto
[] Tidy up documents
[] Make compiler which compile moa to C or LLVM by Haskell
   - number
   - string
   - bool
   - function
   - array
   - struct
   - dictionary
   - struct
   - enum
   - sequence
   - branch
   - loop
[] Self booting
[] Implement remaining features by moa
   - interface
   - generics
   - coroutine
   - namespace (package / import / export)
