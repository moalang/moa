# moa run io.puts 3
def parse moa:
  return []
def infer nodes:
  return 1
def gen nodes:
  return "io.puts(1)"
def main:
  let code io.args().slice(2).join(" ")
  let nodes parse(code)
  infer nodes
  io.puts gen(nodes)
